services:
  app:
    build: .
    env_file: .env
    command: ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
    volumes:
      - ./src:/app/src
      - ./credentials:/app/credentials:ro
    ports:
      - "${PORT:-8000}:8000"
